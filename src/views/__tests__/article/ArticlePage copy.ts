// TODO: (code: 0003) Compelete this test
import { describe, it } from 'vitest'

import { mount } from '@vue/test-utils'
import CreateArticlePage from '@/views/article/CreateArticlePage.vue'

describe('', () => {
  it('', () => {
    const wrapper = mount(CreateArticlePage)
  })
})
